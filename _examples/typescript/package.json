{
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.5",
    "typescript": "^5.5.2"
  },
  "scripts": {
    "test": "jest ./src",
    "control": "jest ./control.test.ts --silent --json | jq -c '{passingTestsCount: .numPassedTests, totalTestsCount: .numTotalTests}'"
  }
}
